package nl.kittokazu

const val speedTime:Long = 35 //Speed of the sorting algorithm globally for faster changing, long because bugfix

fun main() {
    javafx.application.Application.launch(MainApp::class.java)
}
