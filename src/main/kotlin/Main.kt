package nl.kittokazu
const val speedTime:Long = 10 //Speed of the sorting algorithm globally for faster changing, long because bugfix

fun main() {
    javafx.application.Application.launch(MainApp::class.java)
}
